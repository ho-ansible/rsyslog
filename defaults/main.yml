---
rsyslog_role: client

rsyslog_server: 127.0.0.1
rsyslog_port: 2514

rsyslog_file: /var/log/messages
rsyslog_host_pattern: "/var/log/%HOSTNAME%/%programname%.log"

# only used by server:
rsyslog_interface: lo
rsyslog_address: 127.0.0.1
rsyslog_tags: []
rsyslog_rules: {}
